@model binarymvc.Models.binarymodelinput

@{
    ViewBag.Title = "GetInput";
}

<html>
<h2>GetInput</h2>
<div class="container">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                @using (Html.BeginForm("GetInput", "Home", FormMethod.Post, new {role = "form"}))
                {
                    <div style="font-weight: bold; color: red">@Html.ValidationSummary()</div>
                    <div class="form-group">
                      <div class="form-group">
                        @Html.LabelFor(m => m.Input)
                        @Html.TextBoxFor(m => m.Input, new
                        {
                            placeholder = "Enter number",
                            @class = "form-control"
                        })
                    </div>
                    <div>
                        
                        }
                    </div>
                 </div>
                    <div>
                        <input type="submit" value="submit" class="btn btn-info">
                    </div>
                }
            </div>
        </div>
    </div>
    </html>